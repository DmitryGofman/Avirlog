# Production dependencies for the AvirLog API — only what server.py imports.
# (The dev requirements.txt carries extra tooling and an Emergent-only package
# that is not on public PyPI; do not use it for deploys.)
fastapi==0.110.1
uvicorn[standard]==0.25.0
motor==3.3.1
pymongo==4.5.0
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
passlib>=1.7.4
bcrypt==4.0.1
python-dotenv>=1.0.1
httpx>=0.24.0
